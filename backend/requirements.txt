# Core dependencies (from CLI)
requests>=2.28.0

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data validation
pydantic>=2.0.0

# Rate limiting
slowapi>=0.1.9

# Database (synchronous - no async needed per original dev feedback)
aiosqlite>=0.19.0
